export const selectMusike = (stor) => {
  return stor.musike.listMusike;
};

export const selectFoto = (stor) => {
  return stor.musike.photo;
};

export const selectTitle = (stor) => {
  return stor.musike.title;
};
